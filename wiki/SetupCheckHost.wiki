#labels Phase-Requirements
= Set up a CheckHost =

The CheckHost Administrator does the following:
  # Install and configure the BOINC client.
  # Add the CentralHost URL to the BOINC client as a project.
  # Optionally he can also select projects to run otherwize all projects are candidates.

If the CheckHost Administrator no longer wants to use his host for this,
he detaches the project or uninstalls the BOINC client.